---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/netbox_auto/__init__.py
  - src/netbox_auto/cli.py
  - .gitignore
  - Makefile
autonomous: true
must_haves:
  truths:
    - "netbox-auto --help displays available commands"
  artifacts:
    - path: "pyproject.toml"
      provides: "Project definition with dependencies"
      contains: "netbox-auto"
    - path: "src/netbox_auto/__init__.py"
      provides: "Package marker"
    - path: "src/netbox_auto/cli.py"
      provides: "CLI entry point"
      contains: "typer"
  key_links:
    - from: "pyproject.toml"
      to: "src/netbox_auto/cli.py"
      via: "scripts entry point"
      pattern: "netbox-auto.*cli:main"
---

<objective>
Create Python project structure with working CLI shell.

Purpose: Establish foundation that all other plans build on - package structure, dependencies, CLI framework.
Output: Installable package with `netbox-auto --help` working.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create project structure with pyproject.toml</name>
  <files>pyproject.toml, src/netbox_auto/__init__.py, .gitignore, Makefile</files>
  <action>
    Create pyproject.toml with:
    - name: netbox-auto
    - Python 3.11+ requirement
    - Dependencies: typer[all], pyyaml, pydantic, pydantic-settings, sqlalchemy
    - Dev dependencies: black, ruff, mypy, pytest
    - Entry point: netbox-auto = "netbox_auto.cli:main"

    Create src/netbox_auto/__init__.py with __version__ = "0.1.0"

    Create .gitignore with Python defaults + config.yaml, *.db

    Create Makefile with targets: format, lint, type, test, ci (runs all)
  </action>
  <verify>cat pyproject.toml shows correct structure</verify>
  <done>pyproject.toml exists with all dependencies and entry point</done>
</task>

<task type="auto">
  <name>Task 2: Create CLI with Typer</name>
  <files>src/netbox_auto/cli.py</files>
  <action>
    Create CLI using Typer with:
    - main() function as entry point with app = typer.Typer()
    - Placeholder commands: discover, serve, push, status (each just prints "Not implemented yet")
    - Version callback showing __version__
    - Help text describing the tool

    Use typer.Typer(help="Network discovery and NetBox population tool")
  </action>
  <verify>pip install -e . && netbox-auto --help</verify>
  <done>netbox-auto --help shows all four commands with descriptions</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pip install -e .` succeeds
- [ ] `netbox-auto --help` displays available commands
- [ ] `netbox-auto --version` shows 0.1.0
- [ ] `netbox-auto discover` runs (prints placeholder)
- [ ] `make ci` passes (format, lint, type checks)
</verification>

<success_criteria>

- Package installs without errors
- CLI entry point works
- All placeholder commands accessible
- Code passes linting and type checks
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
